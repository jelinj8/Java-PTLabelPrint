package cz.bliksoft.ptlabelprint.protocol.phomemo;

/** How a model's paper roll is aligned relative to the printhead - ported from phomymo's per-model {@code alignment} field. */
public enum PhomemoAlignment {
	LEFT,
	CENTER,
	RIGHT
}
