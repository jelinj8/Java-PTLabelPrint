package cz.bliksoft.ptlabelprint.protocol.niimbot;

/** How a model's printhead is oriented relative to the label feed direction. Affects image rotation before sending (not yet ported). */
public enum PrintDirection {
	LEFT,
	TOP
}
