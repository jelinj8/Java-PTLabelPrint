<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 https://maven.apache.org/xsd/assembly-1.1.3.xsd">

  <id>cli-dist</id>

  <formats>
    <format>dir</format>
    <format>zip</format>
  </formats>

  <includeBaseDirectory>false</includeBaseDirectory>

  <!-- Copy and rename the built jar to ptlabelprint-cli.jar -->
  <files>
    <file>
      <source>${project.build.directory}/${project.build.finalName}.jar</source>
      <outputDirectory>/</outputDirectory>
      <destName>ptlabelprint-cli.jar</destName>
    </file>
  </files>

  <fileSets>
    <!-- lib folder (already copied by maven-dependency-plugin's copy-cli-dist-deps execution) -->
    <fileSet>
      <directory>${project.build.directory}/dist/lib</directory>
      <outputDirectory>/lib</outputDirectory>
      <includes>
        <include>**/*</include>
      </includes>
    </fileSet>

    <!-- launch scripts -->
    <fileSet>
      <directory>src/assembly/scripts</directory>
      <outputDirectory>/</outputDirectory>
      <includes>
        <include>**/*</include>
      </includes>
      <fileMode>0755</fileMode>
    </fileSet>
  </fileSets>

</assembly>
